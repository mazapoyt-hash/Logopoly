{
  "name": "coinscope",
  "version": "0.1.0",
  "description": "CoinScope — crypto signal scanner with honest, verified track record",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "backtest": "node server/cli-backtest.js",
    "replay": "node server/cli-replay.js",
    "test": "node test/run.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.21.0",
    "lightweight-charts": "^4.2.0"
  }
}
